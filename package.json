{
  "name": "douyin-skill",
  "version": "3.2.0",
  "description": "Douyin (抖音) creator account data collector — browser-native API interception, no signing required",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "collect-all": "node scripts/batch_collect.mjs",
    "dashboard": "node scripts/dashboard.mjs",
    "setup": "npm install && npx playwright install chromium",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.60.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
